# MongoDB connection string (MongoDB Atlas or local mongod)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/attendance-tracker

# Secret used to sign login session tokens - use a long random string in production
JWT_SECRET=change-this-to-a-long-random-secret-string

# Name of the database (optional if included in MONGODB_URI)
MONGODB_DB=attendance-tracker
